# Vite client-side env vars (exposed to the browser — never put secrets here).
# Base URL of the Node API. In dev, Vite proxies /api → http://localhost:3000/api
# via vite.config.js, so this can be a relative path. In production, set to
# the absolute URL of the deployed API.
VITE_API_BASE=/api
